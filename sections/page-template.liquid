<h1>{{page.title}}</h1>
<div>{{page-content}}</div>

{% schema %}
{
    "name": "Paage settings",
    "settings": [
        {
            "type": "select",
            "id": "container",
            "label": "Layout",
            "options": [
            {
                "value": "container",
                "label": "Container"

            },
        {
            "value":"container-fluid",
            "label": "Conatiner Fluid"
        }  
        ],
        "default": "container"
        }
    ]
}
{% endschema %}